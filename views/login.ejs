<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
    <title>Login</title>
    <style>
        body{
            background:linear-gradient(rgb(0, 174, 255),white, rgb(0, 174, 255));
            background-repeat: no-repeat;
        }
        .container{
            width: 40%;
            margin: 5em auto 16em auto;
            display: flex;
            flex-direction: column;
            background: transparent;
            box-shadow: 1px 2px 2px 3px rgba(145, 144, 144, 0.548);
            border-radius: 4px;
        }
        .container .header{
            text-align: center;
        }
        .header i{
            font-size: 5rem;
            margin: 0.3em 0 0em 0;

        }
        .header h5{
            margin: 0;
            font-size: 3rem;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        form{
            display: flex;
            flex-direction: column;
            padding: 2em 5em 4em 5em; 
        }
        form input, button{
            margin: 0.5em 0;
            padding: 0.7em;
        }
        form button{
            background:rgb(0, 174, 255) ;
            outline: none;
            border-radius: 4px;
            color: white;
            font-size: 1.3rem;
            padding: 0.2em;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <i class="fas fa-user-circle"></i>
            <h5>Log in</h5>
        </div>
        <form action="/home" method="POST">
            <input type="text" placeholder="Username">
            <input type="text" placeholder="password">
            <button type="submit">Log in</button>
        </form>
    </div>
</body>
</html>